# Archived Notebooks

Historical or legacy notebooks kept for reference:

| Notebook | Notes |
| --- | --- |
| `dpo.ipynb` | Legacy DPO path. DPO repeatedly collapsed and should not be the default path. |
| `preprocess.ipynb` | Old standalone preprocessing path. Current training preprocessing is inline in `../training.ipynb`. |
| `finetune_local.ipynb` | Old local fine-tuning experiment. |

