# Local development + scripts. Kaggle notebooks pin their own deps inline.

# Core ML
torch>=2.1
transformers>=4.40
peft>=0.10
trl>=0.8           # ORPOTrainer requires 0.8+
accelerate>=0.30
datasets>=2.18
bitsandbytes>=0.43; sys_platform == 'linux'   # 8-bit optim, GPU only

# HF + Kaggle integration
huggingface_hub>=0.23
kagglehub>=0.2

# Distillation script (eval/distill_responses.py)
httpx>=0.27

# Local bench notebook UI
ipywidgets>=8.0

# Notebook tooling
jupyter
nbstripout         # strip outputs on commit (optional: `nbstripout --install`)

# Optional: llama-cpp-python for GGUF inference inside the bench notebook
# (transformers backend is the default; install only if you want gguf backend)
# llama-cpp-python>=0.2
